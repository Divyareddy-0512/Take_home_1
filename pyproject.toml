[project]
name = "verilog-tests"
version = "0.1.0"
description = "Verilog testing with cocotb"
requires-python = ">=3.10"
dependencies = [
    "pytest>=8.0.0",
    "cocotb>=1.8.0",
    "cocotb-test>=0.2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
